for ( int i = N; i >= 8; i >>= 8 ) { // i >>= 8 -> i /= 2^8 -> i /= 256 LogN
    for ( int i = N - 272; i < N - 200; i += 50 ) { // O(1)
        printf("Hello!\n");
    }
}

-72 + 50 = -22

-22 + 50 = 28

value % div = 0...div-1

for ( int i = N + 195; i <= N + 361; i += 37 ) { // O(1)
    for ( int i = N + 466; i >= -18; i -= N % 81 + 1 ) { // O(N)
        printf("Hello!\n");
    }
}


// 0..175
for ( int i = -2; i < N - 247; i += 81 ) { // O(N)
    for ( int i = N % 176; i >= 10; i -= 3 ) { // O(1)
        printf("Hello!\n");
    }
}

// 0..176+1
for ( int i = 1; i <= N; i *= 2 ) { // LogN
    for ( int i = N; i > 0; i -= 1 ) { // N
        printf("Hello!\n");
    }
}

for ( int i = 0; i <= 280; i += 1 ) { // O(1)
    for ( int i = -N; i < N; i += 1 ) { // O(N)
        printf("Hello!\n");
    }
}

for ( int i = N; i > 1; i >>= 1 ) { // LogN
    for ( int i = 2; i <= N; i <<= 2 ) { // LogN
        printf("Hello!\n");
    }
}

for ( int i = 6; i <= N; i *= 170 ) { // LogN
    for ( int i = 7; i <= N + 199; i *= 138 ) { // LogN
        printf("Hello!\n");
    }
}

// 0..147
for ( int i = N % 148; i >= 6; i -= 54 ) { // O(1)
    for ( int i = -60; i < N % 180; i += 65 ) { // O(1)
        printf("Hello!\n");
    }
}


for ( int i = N - 341; i > -69; i -= 132 ) { // O(N)
    for ( int i = 300 - N; i < N - 256; i += 82 ) { // O(N)
        printf("Hello!\n");
    }
}


for ( int i = N; i >= 3; i /= 221 ) { LogN
    for ( int i = N - 311; i > 386 - N; i -= 121 ) { N
        printf("Hello!\n");
    }
}

for ( int i = - N; i <= N ; i += 0 + 1 ) { // O(N)
    for ( int i = N ; i > - N; i -= 0 + 1 ) { // O(N)
        printf("Hello!\n");
    }
}